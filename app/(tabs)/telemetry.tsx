// app/(tabs)/telemetry.tsx
import { View, Text } from 'react-native';

export default function TelemetryScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Telemetry Screen</Text>
    </View>
  );
}